// import { useRelatedProducts } from "./useRelatedProducts"

const RelatedProducts = () => {
    // const { isPending, relatedProduct } = useRelatedProducts()
    // console.log(relatedProduct);
    
    return (
        <div></div>
    )
}

export default RelatedProducts